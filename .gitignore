bin/
obj/
.vs/
.vscode/
TestResults/
*.user
*.suo
*.cache
*.pdb

# EF Core migration bundle — generated on every deploy by .github/workflows/cd.yml.
# Local builds via `dotnet ef migrations bundle` also drop it here.
efbundle.exe
